存在问题:
  meShow: 
    内部的服务接口需要迁移到模块中
  模块(server): 
    redis的公共配置按照业务模块统一配置(已完成)
    数据库的公共配置 主从配置 多租户配置(已完成)
  小程序(project): 
    暂不支持新增租户不用重启的配置
  disconf多租户差异化配置:
    目前考虑在有差异化的配置项新增不同租户对应不同的配置值 例如：
    # 倔强青铜勋章id
    stubbornBronze.medalId={{0,9640},{1,7000}} 0,1是租户id

多租户公共配置测试:
  redis 已接入
  mq 已接入
  db 已接入
  ejob 已接入
  ehcache 初始化实例对象加入租户id变量 每个租户对应不同的内存缓存对象实例 key值保持一样不变 已接入
  meShow 已接入

